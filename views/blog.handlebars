<div class="text-center">
  <h2>{{title}}</h2>
    <p>{{article}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <button class="btn btn-primary">Blog it!</button>
  </p>
</div>
